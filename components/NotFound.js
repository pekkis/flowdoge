var React = require('react');

var NotFound = React.createClass({

    render: function() {
        return (
            <div>
                O-ou 404
            </div>
        );
    }

});

module.exports = NotFound;